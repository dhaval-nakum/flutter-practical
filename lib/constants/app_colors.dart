import 'package:flutter/material.dart';

// A utility class for defining app-wide color constants.
class AppColors {
  static const Color primaryColor = Colors.blue; // Primary color used in the app.
  static const Color whiteColor = Colors.white; // White color for text or backgrounds.

  static const Color greenColor = Color(0xff4caf50);
  static const Color redColor = Color(0xffab0a0a);
}
